@model IEnumerable<BEWebApp2.Models.Order>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Quản lí đơn hàng </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã Đơn HHàng
        </th>
        <th>
            Tên KH
        </th>
        <th>
            Ngày mua
        </th>
        <th>
            Tổng Giá Trị
        </th>
        @*<th>
               Mã Khánh Hàng
            </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.OrderID)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Customer.CusName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalValue)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Customer.CusPassword)
                </td>*@
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.OrderID }) |*@
                @Html.ActionLink("chi tiết", "Details", new { id = item.OrderID } , new { style = "background-color: #8ac7d9; color: white; padding: 10px 20px; border: none; border-radius: 5px;" }) 
                @*@Html.ActionLink("Delete", "Delete", new { id = item.OrderID })*@
            </td>
        </tr>
    }

</table>
